-
  name: pb on use case of loop
  hosts: client1
  tasks:
   - name: below code for yml installations using loop
     yum:
      name: "{{ item}}"
      state: present
     with_items:
     - git
     - docker
     - net-tools
     - finger

    
